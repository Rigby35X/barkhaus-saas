---
import AnimalCard from "./AnimalCard.astro";import {getPublicAnimals} from "../../lib/public-animals";const animals=(await getPublicAnimals(Astro.locals)).slice(0,4);
---
<div class="grid grid-4">{animals.map(animal=><AnimalCard animal={animal}/>)}</div>